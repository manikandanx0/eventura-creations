MONGODB_URI=mongodb://127.0.0.1:27017/eventura
PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Required in production. In development, the server falls back to a dev secret if unset.
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d
